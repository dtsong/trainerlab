# Backend environment variables
# Copy to .env and fill in values

# Environment
ENVIRONMENT=development
DEBUG=true

# Database (PostgreSQL with pgvector)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/trainerlab

# Redis
REDIS_URL=redis://localhost:6379

# TCGdex (self-hosted)
TCGDEX_URL=http://localhost:3001

# OpenAI (for embeddings - optional for initial dev)
# OPENAI_API_KEY=sk-...

# Auth (Firebase)
# See docs/FIREBASE_SETUP.md for setup instructions
FIREBASE_PROJECT_ID=
# Path to service account JSON (for local dev)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/firebase-service-account.json
