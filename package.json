{
  "name": "trainerlab",
  "version": "0.0.1",
  "private": true,
  "description": "Competitive intelligence platform for Pokemon TCG",
  "scripts": {
    "dev": "pnpm --filter @trainerlab/web dev",
    "build": "pnpm --filter @trainerlab/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@9.15.0"
}
