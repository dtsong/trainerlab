# Production environment variables
# Used by Vercel for production deployments

# API URL (FastAPI backend on Cloud Run)
NEXT_PUBLIC_API_URL=https://api.trainerlab.io

# Auth (NextAuth.js)
# Configure NEXTAUTH_SECRET, AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET
# in Vercel project settings — do NOT commit secrets
# NEXTAUTH_URL is NOT set here — Vercel auto-detects it
